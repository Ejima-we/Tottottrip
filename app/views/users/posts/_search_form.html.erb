  <%= form_with model:post, url: search_path, method: :get, local: true do |f| %>
    <%= f.select :keyword, [ ["投稿が新しい順", "new"], ["投稿が古い順", "old"], ["いいねが多い順", "favorites"]] %>
    <%= f.submit "検索" %>
  <% end %>